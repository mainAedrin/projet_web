# Copiez ce fichier en .env et ajustez les valeurs
# cp .env.example .env

# ─── Base de données ─────────────────────────────────────────────
DB_ROOT_PASS=changeme_root
DB_NAME=cv_platform
DB_USER=cv_user
DB_PASS=changeme_user
